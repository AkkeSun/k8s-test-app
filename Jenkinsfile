pipeline {
    agent any

    stages {
        stage('[Master] Jenkins variable setting') {
            when {
                branch 'master'
            }
            steps {
                echo 'cicd 1'
            }
        }
    }
}
